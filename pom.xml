<project>
<modelVersion>4.0.0</modelVersion>
<groupId>com.canyonmodded</groupId>
<artifactId>canyon</artifactId>
<version>1.7.3</version>
<!-- 👇 关键：改成 jar，生成可执行包 -->
<packaging>jar</packaging>
<name>Canyon</name>

<build>
  <defaultGoal>install</defaultGoal>
  <finalName>Canyon</finalName>
  <plugins>
    <!-- 必须加：Java8 编译插件 -->
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-compiler-plugin</artifactId>
      <version>3.8.1</version>
      <configuration>
        <source>8</source>
        <target>8</target>
        <encoding>UTF-8</encoding>
      </configuration>
    </plugin>
  </plugins>
</build>

<!-- 👇 关键：删除这整个 modules 模块 -->
<!-- <modules>
<module>Canyon-API</module>
<module>Canyon-Server</module>
</modules> -->

</project>
